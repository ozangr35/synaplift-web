/** Public site constants (no secrets). */
export const siteUrl = "https://synaplift.com";

export const supportEmail = "synaplift@outlook.com";

/** Set when App Store / Play listings are live. */
export const appStoreUrl: string | null = null;
export const playStoreUrl: string | null = null;
