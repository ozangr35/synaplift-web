# MYON marketing site (static — no database)

```bash
# From repo root:
./web/start.sh

# Or:
cd web && npm run dev
```

→ **http://localhost:3000**

Pages: Home, About, Contact, Privacy, Terms.

Deploy: Vercel root = `web/frontend`.
